{
  "name": "ceegee",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter engine-ui dev",
    "build": "pnpm --filter engine-ui build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
